#version 330 core

layout (location = 0) in vec3 position;

uniform	mat4 view;
uniform	mat4 projection;

void main()
{
	vec4 clipSpacePosition = (vec4(position.xyz, 1) * view) * projection;
	gl_Position = clipSpacePosition;
}